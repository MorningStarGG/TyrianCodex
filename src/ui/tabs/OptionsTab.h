#pragma once
class App;

// Options tab: a sectioned settings view (left section menu + search, right filtered controls)
// plus the embedded Diagnostics section. Defined in ui/tabs/OptionsTab.cpp.
void DrawOptionsContent(App &app);
